[build]
  base = "app"
  command = "npm ci && npm run build"
  publish = "dist"
  functions = "../netlify/functions"

[build.environment]
  NODE_VERSION = "22"


[dev]
  command = "npm run dev"
  port = 3000
